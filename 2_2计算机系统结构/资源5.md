第五章 线程级并行
  请在PVP、SMP、MPP、DSM和COW中任选一种，简要描述其特点。

并行向量处理机（PVP）：
这样的系统中包含了少量的高性能专门设计定制的向量处理器 ＶＰ，每个至少具有１Ｇflops的处理能力；

存储器以兆字节每秒的速度向处理器提供数据；

向量处理器VP和共享存储模块通过高带宽的交叉开关网络互连；

通常不使用高速缓存，而是使用大量的向量寄存器和指令缓冲器；

从存储角度来看MIMD（22级简答题最后一道，只有qgn老师班讲了这个）

（1）单地址空间共享存储
           a. 均匀存储访问：PVP、SMP
           b. 非均匀存储访问：DSM
（2）多地址空间非共享存储：MPP、COW
 
什么是多处理机Cache一致性问题？

允许共享数据进入Cache，就可能出现多个处理器的Cache中都有同一存储块的副本，当其中某个处理器对其Cache中的数据进行修改后，就会使得其Cache中的数据与其他Cache中的数据不一致。
 
  请简述监听式协议的原理。

Cache通常连在共享存储器的总线上，当某个Cache需要访问存储器时，它会把请求放到总线上广播出去，其他各个Cache控制器通过监听总线（它们一直在监听）来判断它们是否有总线上请求的数据块。如果有，就进行相应的操作。
 
  请简述目录式协议的原理。

将物理存储器中数据块的共享状态被保存在一个称为目录的地方用于共享。
 
  请比较说明写作废协议和写更新协议性能上的差别。

在对同一个数据进行多次写操作而中间无读操作的情况下，写更新协议需进行多次写广播操作，而写作废协议只需一次作废操作。

在对同一Cache块的多个字进行写操作的情况下，写更新协议对于每一个写操作都要进行一次广播，而写作废协议仅在对该块的第一次写时进行作废操作即可。

写作废是针对Cache块进行操作，而写更新则是针对字（或字节）进行。

考虑从一个处理器A进行写操作后到另一个处理器B能读到该写入数据之间的延迟时间。写更新协议的延迟时间较小。

  请解释，在目录式协议中，什么是本地节点、宿主节点、远程节点和共享集合？

本地节点：发出访问请求的节点
宿主节点：包含所访问的存储单元及其目录项的节点
远程节点：Cache中拥有该块的副本
共享集合：位向量记录拥有其副本的处理器的集合
 
请简述目录式协议中，目录的三种结构。

全映像目录：每一个目录项都包含一个N位的位向量，其每一位对应于一个处理机
有限映像目录：采用位数固定的目录项目，通过对同一数据块在所有cache中的副本总数进行限制来实现。
链式目录：用一个目录指针链表来表示共享集合。当一个数据块的副本数增加（或减少）时，其指针链表就跟着变长（或变短）
————————————————
版权声明：本文为CSDN博主「GineLee」的原创文章，遵循CC 4.0 BY-SA版权协议，转载请附上原文出处链接及本声明。
原文链接：https://blog.csdn.net/m0_73654355/article/details/145062466


1. (简答题)
请在PVP、SMP、MPP、DSM和COW中任选一种，简要描述其特点。

我的答案：
并行向量处理机（PVP）：

（1）这样的系统中包含了少量的高性能专门设计定制的向量处理器 ＶＰ，每个至少具有１Ｇflops的处理能力；

（2）存储器以兆字节每秒的速度向处理器提供数据；

（3）向量处理器VP和共享存储模块通过高带宽的交叉开关网络互连；

（4）通常不使用高速缓存，而是使用大量的向量寄存器和指令缓冲器；



正确答案：
2. (简答题)
什么是多处理机Cache一致性问题？

我的答案：
允许共享数据进入Cache，就可能出现多个处理器的Cache中都有同一存储块的副本，当其中某个处理器对其Cache中的数据进行修改后，就会使得其Cache中的数据与其他Cache中的数据不一致。

正确答案：
3. (简答题)
请简述监听式协议的原理。

我的答案：
Cache通常连在共享存储器的总线上，当某个Cache需要访问存储器时，它会把请求放到总线上广播出去，其他各个Cache控制器通过监听总线（它们一直在监听）来判断它们是否有总线上请求的数据块。如果有，就进行相应的操作。

正确答案：
4. (简答题)
请简述目录式协议的原理。

我的答案：
将物理存储器中数据块的共享状态被保存在一个称为目录的地方用于共享。

正确答案：
5. (简答题)
请比较说明写作废协议和写更新协议性能上的差别。

我的答案：
（1）在对同一个数据进行多次写操作而中间无读操作的情况下，写更新协议需进行多次写广播操作，而写作废协议只需一次作废操作。

（2）在对同一Cache块的多个字进行写操作的情况下，写更新协议对于每一个写操作都要进行一次广播，而写作废协议仅在对该块的第一次写时进行作废操作即可。

（3）写作废是针对Cache块进行操作，而写更新则是针对字（或字节）进行。

（4）考虑从一个处理器A进行写操作后到另一个处理器B能读到该写入数据之间的延迟时间。写更新协议的延迟时间较小。

正确答案：
6. (简答题)
请解释，在目录式协议中，什么是本地节点、宿主节点、远程节点和共享集合？





我的答案：
本地节点：发出访问请求的节点
宿主节点：包含所访问的存储单元及其目录项的节点
远程节点：Cache中拥有该块的副本
共享集合：位向量记录拥有其副本的处理器的集合

正确答案：
二. 填空题（共1题）
7. (填空题)
假设某多机系统为共享主存均匀存储访问系统，采用监听式Cache一致性协议，配置如下：

（1）存储器和Cache均按照字节编址；

（2）一共有4个处理节点，分别为CPU00，CPU01，CPU10，CPU11；

（3） 每个节点的Cache块数为16块，块大小为16B，直接映射；

（4）存储器为统一编址，容量为16MB；

（5）采用写作废协议和写回法；

（6）Cache块的三种状态定义如下：Invalid（00），Shared（10），Modified（11）；

（7）当ReadMiss或者WriteMiss时，数据采用就近提供原则，即若缓存中有，则由缓存提供，否则，由主存提供。

（8）某时刻，各个节点Cache的状态如下。

CPU00

CPU01

CPU10

CPU11

块号

标记

状态

块号

标记

状态

块号

标记

状态

块号

标记

状态

0H

12FFH

00

0H

0023H

11

0H

0124H

10

0H

8A7FH

00

1H

28BEH

10

1H

34EAH

10

1H

28BEH

10

1H

34EAH

10

请按照访存序列完成下表，答题要求：

（1）          请求包括ReadHit/ReadMiss/WriteHit/WriteMiss；

（2）          转换起点和终点中间用空格，比如00 10

（3）          本地操作从下面四个中选一个填序号（如填1）

将ReadMiss放到总线上
将WriteMiss放到总线上
将Invalidate放总线上
读本地数据
 写本地数据




访存请求1：CPU00读：002302H

本地请求

（1）

状态转换

（2）

本地操作

（3）

访存请求2：CPU01写：34EA1FH

本地请求

（4）

状态转换

（5）

本地操作

（6）

访存请求3：CPU10读：28BE1AH

本地请求

（7）

状态转换

（8）

本地操作

（9）

访存请求4：CPU11写：28BE11H

本地请求

（10）

状态转换

（11）

本地操作

（12）



访存动作结束后，四个节点Cache状态为（请填写十六进制数+H）：

CPU00

CPU01

CPU10

CPU11

块号

标记

状态

块号

标记

状态

块号

标记

状态

块号

标记

状态

0H

（13）

（14）

0H

（17）

（18）

0H

（21）

（22）

0H

（25）

（26）

1H

（15）

（16）

1H

（19）

（20）

1H

（23）

（24）

1H

（27）

（28）



我的答案：
(1)
ReadMiss

(2)
00 10

(3)
1

(4)
WriteHit

(5)
10 11

(6)
3

(7)
ReadHit

(8)
10 10

(9)
4

(10)
WriteMiss

(11)
00 11

(12)
2

(13)
0023H

(14)
10

(15)
28BEH

(16)
00

(17)
0023H

(18)
10

(19)
34EAH

(20)
11

(21)
0124H

(22)
10

(23)
28BEH

(24)
00

(25)
8A7FH

(26)
00

(27)
28BEH

(28)
11